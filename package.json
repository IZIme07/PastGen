{
  "name": "pastgen",
  "version": "1.0.0",
  "private": true,
  "description": "PastGen — семейная история: из хаоса в проверяемое дерево",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm-run-all -p dev:server dev:client",
    "dev:server": "npm -w server run dev",
    "dev:client": "npm -w client run dev",
    "build": "npm -w client run build && npm -w server run build",
    "test": "npm -w server run test && npm -w client run test",
    "seed": "npm -w server run seed",
    "start": "npm -w server run start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=22"
  }
}
